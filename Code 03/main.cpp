#include "Principal.h"
//Este codigo e referente aos slides n 03A.
//Comentei pouco o codigo, pq acho q por ele ser tao separado se torna facil identificar oq cada parte faz
//Nao consegui fazer a atualizacao do codigo do conteudo referente a heranca (slide 3B e 3C).
int main(){
    Principal sistemaAcademico;
    sistemaAcademico.inicializar();

    return 0;
}
